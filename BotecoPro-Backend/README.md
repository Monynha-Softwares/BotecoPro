# BotecoPro Backend

SQL scripts and Supabase configuration for the BotecoPro project.

The `database/supabase` folder contains base schemas and RLS policies. Execute these scripts using Supabase CLI or `psql` to bootstrap a new environment.
